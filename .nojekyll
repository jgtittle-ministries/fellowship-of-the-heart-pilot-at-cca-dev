# This file disables Jekyll processing on GitHub Pages so that files and
# folders beginning with underscores (and the raw docs/ markdown) are served
# as-is. Required for the static reader. Keep this file at the repo root.
